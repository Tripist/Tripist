<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tripist">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".controller.categories.Bazaar_MarketsCategory"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"/>
        <activity
            android:name=".controller.categories.SquaresCategory"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"/>
        <activity
            android:name=".controller.categories.ReligionsCategory"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"/>
        <activity
            android:name=".controller.categories.ParksCategory"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"/>
        <activity
            android:name=".controller.categories.FoodsCategory"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"/>
        <activity
            android:name=".controller.categories.Island_BeachesCategory"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"/>
        <activity
            android:name=".controller.categories.MuseumsCategory"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity
            android:name=".controller.categories.HistoricalPlacesCategory"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity android:name=".controller.OnBoarding" />
        <activity
            android:name=".controller.BottomNav"
            android:label="@string/title_activity_bottom_nav" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".controller.maps.My_Favourites"
            android:label="@string/title_activity_my__favourites" />
        <activity
            android:name=".controller.maps.My_Locations"
            android:label="@string/title_activity_my__locations" />
        <activity
            android:name=".controller.maps.Squares"
            android:label="@string/title_activity_squares" />
        <activity
            android:name=".controller.maps.Bazaar_Markets"
            android:label="@string/title_activity_bazaar__markets" />
        <activity
            android:name=".controller.maps.Religions"
            android:label="@string/title_activity_religions" />
        <activity
            android:name=".controller.maps.Island_Beachs"
            android:label="@string/title_activity_island__beachs" />
        <activity
            android:name=".controller.maps.Parks"
            android:label="@string/title_activity_parks" />
        <activity
            android:name=".controller.maps.Historical_Places"
            android:label="@string/title_activity_historical__places" />
        <activity
            android:name=".controller.maps.Museums"
            android:label="@string/title_activity_museums" />
        <activity android:name=".controller.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>